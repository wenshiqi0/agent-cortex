# agent-cortex

The complete shared rules for this repo live in **`knowledge/AGENTS.md`**
(relative to this file). Read that file now and follow it as the authoritative
ruleset before doing anything else in this repository — it defines how skills,
agents, and rules are organized, the Loop for developing subprojects under
`repositories/`, and the conventions every tool must obey.

This file is intentionally a thin pointer so the single source of truth stays in
`knowledge/AGENTS.md`. Do not duplicate rules here; open `knowledge/AGENTS.md`.
