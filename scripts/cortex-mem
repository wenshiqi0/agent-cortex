#!/usr/bin/env sh
# cortex-mem — local Qdrant-backed memory entrypoint.
set -e
DIR=$(cd "$(dirname "$0")" && pwd)
command -v bun >/dev/null 2>&1 || { echo "cortex-mem: bun not found on PATH (https://bun.sh)" >&2; exit 1; }
exec bun "$DIR/cortex-mem.js" "$@"
